% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpaca_vcov.R
\name{alpaca_vcovSandwich}
\alias{alpaca_vcovSandwich}
\title{Heteroskedasticity-robust variance-covariance matrix for alpaca feglm}
\usage{
alpaca_vcovSandwich(mod)
}
\arguments{
\item{mod}{A fitted \code{feglm} object from the \pkg{alpaca} package.}
}
\value{
A square diagonal matrix of class \code{"vcovAlpacaSandwich"} with
  squared sandwich standard errors on the diagonal.
}
\description{
Wraps \code{summary(mod, type = "sandwich")} and returns a diagonal
variance-covariance matrix that \code{\link{stargazer}} recognises and
labels as "heteroskedasticity-robust standard errors".
}
\examples{
\donttest{
if (requireNamespace("alpaca", quietly = TRUE)) {
  d <- data.frame(
    y   = rbinom(200, 1, 0.5),
    x1  = rnorm(200),
    grp = factor(rep(1:10, 20))
  )
  mod <- alpaca::feglm(y ~ x1 | grp, d, binomial("logit"))
  V   <- alpaca_vcovSandwich(mod)
  stargazer(mod, vcov = list(V), type = "text")
}
}
}
